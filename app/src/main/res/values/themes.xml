<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash screen starting theme — transitions to main theme after splash -->
    <style name="Theme.ExpenseTracker.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/teal_600</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ExpenseTracker</item>
    </style>

    <!-- Main app theme (Compose handles visual theming; this just sets window chrome) -->
    <style name="Theme.ExpenseTracker" parent="android:Theme.Material.Light.NoActionBar" />

</resources>
